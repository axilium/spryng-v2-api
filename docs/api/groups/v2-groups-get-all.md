# Groups / Get all

Source: <https://developer.spryng.nl/groups/v2-groups-get-all>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint allows you to retrieve a list of all groups associated with your account in a single API call. The request can be filtered by: Name, sortBy, sortOrder, groupOwnership, StartTime, EndTime as well as other properties listed below. This is the ideal tool for synchronizing your contact list from another system, such as a CRM, an e-commerce platform, or a customer database, ensuring your contacts are always up-to-date.

Before you start using this endpoint, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

```
https://api.spryng.nl/v2/groups
```

**Method: GET**

## Headers

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| X-Api-Key | String | API Key value for authorization |
| AccountReference | String | Your Spryng account reference - e.g. SPNL0000000 |

**Example request header:**

```
GET https://api.spryng.nl/v2/groups
-H 'Api-Key: YOUR_API_KEY'
-H 'AccountReference: SPNL0000000'
```

**Optional input path parameters**

The below parameters are optional within the path, they are designed to provide you further capability to filter your requests.

| Parameter | Type | Description |
| --- | --- | --- |
| groupId | GUID | Filter by groupIds |
| name | Query | Filter by the name of the group |
| groupOwnership | Query | Specifies the ownership type of a group to filter by. Possible values: Shared \| Owned \| All. Default value = All |
| sortBy | Query | Sort groups. Available values: name \| createdAt |
| sortOrder | Query | Sets the sorting direction. Default = descending. Available values: Asc & Desc |

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide.](https://restfulapi.net/http-status-codes/)

**Success**

```
200 OK
```

Success response.

```
{
    "data": {
        "groups": [
            {
                "groupId": "4d27b28b-48ce-40d1-8500-0751986ca623",
                "accountReference": "SPNL0000000",
                "name": "Octopus",
                "description": "A group description for test purpose",
                "contactsCount": 0,
                "lastUpdatedBy": "907da4a7-bb93-4772-8af4-a95cc59e5a91",
                "createTimestamp": "2024-11-26T08:36:54.578977Z",
                "lastUpdateTimestamp": "2024-11-26T08:36:54.578977Z"
            },
            {
                "groupId": "549ae49d-fd48-4663-8806-c46e2e4d58b2",
                "accountReference": "SPNL0000000",
                "name": "CDE",
                "description": "group created for test purpose",
                "contactsCount": 1,
                "lastUpdatedBy": "907da4a7-bb93-4772-8af4-a95cc59e5a91",
                "createTimestamp": "2024-11-18T11:15:45.771482Z",
                "lastUpdateTimestamp": "2024-11-18T11:15:45.771482Z"
            }
        ],
        "totalCount": 2,
        "totalPages": 1,
        "currentPage": 1
    }
}
```

If an account doesn't have any groups, a 200 response will be given with the below example body:

```
{
    "data": {
        "groups": [],
        "totalCount": 0,
        "totalPages": 0,
        "currentPage": 1
    }
}
```

**Error codes**

```
400 Bad Request
```

The server could not process the request due to incorrect or missing required properties.
Response example

```
{
    "errors": [
        {
            "errorCode": "error code",
            "errorMessage": "error message"
        }
    ]
}
```

```
404 Not Found
```

The server couldn't process the request due to missing or incorrect groupId.
Response example

```
{
    "errors": [
        {
            "errorCode": "group_not_found",
            "errorMessage": "There is no group with the specified Id."
        }
    ]
}
```

```
401 Unauthorized
```

The server couldn't process the request due to missing or incorrect authorization value.
Response example

```
{
    "title": "An error occurred while processing the request",
    "status": 401,
    "errors": {},
    "internalErrors": [
        {
            "message": "Pipeline",
            "metadata": {
                "unauthenticatedError": "Request for authenticated route '/v2/groups' was unauthenticated"
            }
        }
    ]
}
```

## Code sample: Curl

```bash
curl 'https://api.spryng.nl/v2/groups?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer' -H 'X-Api-Key: {{your-key-here}}' -H 'AccountReference: {{your-AccountReference-header-here}}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/groups";

  url+= "?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer";

  const headers = {
    "AccountReference": "your-AccountReference-header-here",
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  try {
    const response = await axios.get(
        url,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'AccountReference': 'your-AccountReference-header-here',
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/groups"

baseUrl = f"{baseUrl}?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer"


try:
    response = requests.get( baseUrl,  headers = headers )
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/groups";

        baseUrl = baseUrl + "?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer";
        
        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("GET");

        connection.setRequestProperty("AccountReference", "your-AccountReference-header-here");
        
        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/groups";

        url += "?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer";

        var request = new HttpRequestMessage(HttpMethod.Parse("GET"), url);

        request.Headers.Add("AccountReference", "your-AccountReference-header-here");
        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/groups';
$ch = curl_init();

$baseUrl =  $baseUrl . '?AccountReference=String&Name=String&GroupOwnership=String&SortBy=String&StartTime=String&EndTime=String&SortOrder=String&PageNo=Integer&PageSize=Integer';

curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'AccountReference' => 'your-AccountReference-header-here',
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```

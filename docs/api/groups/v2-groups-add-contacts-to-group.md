# Groups / Add contacts

Source: <https://developer.spryng.nl/groups/v2-groups-add-contacts-to-group>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint allows you to add one or more existing contacts to a group in a single API call. This is the ideal tool for synchronizing your contact list from another system, such as a CRM, an e-commerce platform, or a customer database, ensuring your contacts are always up-to-date.

Before you start using this endpoint, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

```
https://api.spryng.nl/v2/groups/{groupId}/contacts
```

**Method: PUT**

## Headers

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| Content-Type | String | application/json |
| X-Api-Key | String | API Key value for authorization |

**Example request header:**

```
PUT https://api.spryng.nl/v2/groups/{groupId}/contacts
-H 'Api-Key: YOUR_API_KEY'
-H 'Content-Type: application/json'
```

## Request body

If a required parameter is not provided, or incorrect, the request will fail. If a parameter is conditional, it is only required for certain channels or is dependent on the desired format of the message.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| contactsId | JSON array | Required | The contactIds to be added to the group provided as an array in the body e.g. "68890cd2-7d9d-43c4-8fc9-8de20160e76f" |

**Request body example**

```
{
    "contactIds": [
        "59152cde-77d2-414b-b616-a3a70c8f2a47" //could be more contacts added here
    ]
}
```

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide.](https://restfulapi.net/http-status-codes/)

**Success**

```
200 OK
```

Success response.

```
{
    "data": {
        "groupContacts": [
            {
                "groupContactId": "c92fd77e-9386-429d-a240-a1c3c380c0dd",
                "groupId": "cb960734-8a57-442d-983c-92526456d3ed",
                "contactId": "ad3db3a0-9989-4ebf-b64b-d12c2c2895d0",
                "lastUpdatedBy": "907da4a7-bb93-4772-8af4-a95cc59e5a91",
                "createTimestamp": "2024-11-26T14:56:36.7986345Z"
            }
        ]
    }
}
```

**Error codes**

```
400 Bad Request
```

The server was unable to process your request due to an invalid or missing required property, i.e. contactId was provided in an invalid format (not GUID).
Response example

```
{
    "errors": [
        {
            "errorCode": "errorCode",
            "errorMessage": "errorMessage"
        }
    ]
}
```

```
404 Not Found
```

The server was unable to process your request due to an invalud or missing groupId.
Response example

```
{
    "errors": [
        {
            "errorCode": "group_not_found",
            "errorMessage": "There is no group with the specified Id."
        }
    ]
}
```

```
401 Unauthorized
```

The authorization header is missing or invalid.
Example response

```
{
    "title": "An error occurred while processing the request",
    "status": 401,
    "message": "Request for authenticated route '/v2/groups/123/contacts' was unauthenticated",
    "errors": {},
    "internalErrors": [
        {
            "message": "Pipeline",
            "metadata": {
                "unauthenticatedError": "Request for authenticated route '/v2/groups/123/contacts' was unauthenticated"
            }
        }
    ]
}
```

## Code sample: Curl

```bash
curl -X PUT 'https://api.spryng.nl/v2/groups/{groupId}/contacts' -H 'X-Api-Key: {{your-key-here}}' -H 'Content-Type: application/json' -d '{
  "contactIds": [
    ""
  ]
}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/groups/%7BgroupId%7D/contacts";

  const headers = {
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  const body = `{
  "contactIds": [
    ""
  ]
}`;
    
  try {
    const response = await axios.put(
        url,
        body,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/groups/%7BgroupId%7D/contacts"



body = """{
  "contactIds": [
    ""
  ]
}"""
try:
    response = requests.put( baseUrl,  data = body)
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/groups/%7BgroupId%7D/contacts";

        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("PUT");

        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        String body = """
        {
  "contactIds": [
    ""
  ]
}""";

        connection.setDoOutput(true);

        try (OutputStream outputStream = connection.getOutputStream()) {
            byte[] input = body.getBytes(StandardCharsets.UTF_8);
            outputStream.write(input, 0, input.length);
        }
        catch (Exception e) {
            System.err.println("Failed due to error: " + e.getMessage());
        }
        
        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/groups/%7BgroupId%7D/contacts";


        var request = new HttpRequestMessage(HttpMethod.Parse("PUT"), url);

        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var body = @"{
  ""contactIds"": [
    """"
  ]
}"; 
        
        request.Content = new StringContent(body, Encoding.UTF8, "application/json");
                
        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/groups/%7BgroupId%7D/contacts';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


    $body = json_encode([
  "contactIds" => [
    ""
  ]
]);
        
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```

# Messaging / Get all by requestId

Source: <https://developer.spryng.nl/messaging/v2-requests-get-messages-from-request>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint returns all messages associated with a single request. This can be filtered based on channel, message type, status and more.

Before you start using this endpoint, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

**Endpoint URL**

```
https://api.spryng.nl/v2/requests/{requestId}/messages
```

**Method: GET**

## Headers & parameters

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Description |
| --- | --- |
| X-Api-Key | API Key value |
| AccountReference | Your Spryng Account reference - e.g. SPNL0000000 |

**Example request header:**

```
GET https://api.spryng.nl/v2/requests/{requestId}/messages
-H 'Api-Key: YOUR_API_KEY'
-H 'AccountReference: SPNL0000000'
```

**Query parameters**

The below parameters are available as optional filters to your request. They should be provided as a query within the request path.

| Property | Type | Description |
| --- | --- | --- |
| AccountReference | Array | The list of account references provided as an array |
| MessageId | String | Filter by a message id |
| Status | String | Status of the requested messages. Enum e.g. Delivered \| Received \| Failed \| In Progress |
| Reason | String | Failure reason provided as an enum e.g. Delivered \| DestinationNotAllowed \| DisallowedContent \| EdgeFailed \| EdgeSubmitted \| FailedToRoute \| Inbound \| MessagePartCountExceeded \| MsisdnInvalid \| OptedOut \| OutsideBusinessHours \| PartiallyDelivered \| senderIdInvalid \| SendingLimitExceeded \| Unprocessable \| WithinQuietHours |
| Msisdn | String | The phone number that received the outbound message or the phone number that sent the inbound message. Must use international format e.g. 447xxxxxxxxx. This lets you filter for all messages from and to an MSISDN |
| DestinationCode | String | Nullable. 2-letter ISO3166 destination code for the destination of the message e.g. Destination sent to/ or destination received from |
| Originator | String | The "from" value for outbound messages or the "to" value for inbound messages. This lets you filter for all messages from and to an originator such as a VMN or Shortcode or senderId |
| RequestName | String | Nullable. Optional name of the request |
| Channel | String | Channel the message was sent via. Enum e.g. SMS |
| MessageType | String | Enum e.g. Unknown \| MarketingAndResearch \| NotificationsAndReminders \| AuthenticationAndSecurity \| PaymentsAndCollections \| InternalOperations \| CustomerSupport \| Conversational |
| Source | String | Which product area provided the message |
| StartTime | dateTime | Filter messages where submittedAt is greater than or equal to startTime |
| EndTime | dateTime | Filter messages where submittedAt is less than or equal to endTime |
| PageSize | Integer | Pagination required for the response. Allow 1-1000. Default = 1 |
| PageNo | Integer | Number of pages for the response. Allow 1+. Default = 1 |
| SortOrder | Query | Defines sorting order for the results. Enum e.g. Asc/ Desc. Default = Desc |
| SortBy | Query | Defines sorting requirements for the results Enum e.g. Status \| submittedAt \| lastupdateTimestamp \| channel \| messageType \| source \| requestName. Default = submittedAt |
| RetrieveMessageBody | Boolean | Provide if message body is required within the response. Default = TRUE |
| Direction | Query | Enum e.g. Outbound/ Inbound |

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide.](https://restfulapi.net/http-status-codes/)

**Success**

```
200 OK
```

Indicates that the request has succeeded.

```
{
    "data": {
        "messages": [
            {
                "messageId": "0e61a159-b8f8-46db-9f5f-f124006cb967",
                "requestId": "089ef80f-1d6b-4825-a2ec-f9cae16332c4",
                "accountReference": "SPNL0000000",
                "requestName": "V22",
                "source": "Titan-API",
                "channel": "SMS",
                "messageType": "MarketingAndResearch",
                "originator": "71460491",
                "isBillable": true,
                "msisdn": "+447711000032",
                "status": "Failed",
                "reason": "senderIdInvalid",
                "destinationCode": "GB",
                "messagePartCount": 1,
                "submittedAt": "2024-10-17T07:13:03.547255Z",
                "lastUpdateTimestamp": "2024-10-17T07:13:05.291014Z",
                "direction": "Outbound"
            }
        ],
        "totalCount": 1,
        "currentPage": 1,
        "totalPages": 1
    }
}
```

**Error codes**

```
400 Bad Request
```

Indicates the requestId is missing or invalid.

```
{
    "errors": [
        {
            "errorCode": "invalid_request_id",
            "errorMessage": "Invalid empty request id"
        }
    ]
}
```

```
401 Unauthorized
```

Indicates the authorization header is missing or invalid.

```
{
    "errors": [
        {
            "errorCode": "unauthenticatedError",
            "errorMessage": "Request for authenticated route '/v2/requests/089ef80f-1d6b-4825-a2ec-f9cae16332c1' was unauthenticated'
        }
    ]
}
```

## Code sample: Curl

```bash
curl 'https://api.spryng.nl/v2/requests/{requestId}/messages?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String' -H 'X-Api-Key: {{your-key-here}}' -H 'AccountReference: {{your-AccountReference-header-here}}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/requests/%7BrequestId%7D/messages";

  url+= "?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String";

  const headers = {
    "AccountReference": "your-AccountReference-header-here",
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  try {
    const response = await axios.get(
        url,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'AccountReference': 'your-AccountReference-header-here',
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/requests/%7BrequestId%7D/messages"

baseUrl = f"{baseUrl}?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String"


try:
    response = requests.get( baseUrl,  headers = headers )
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/requests/%7BrequestId%7D/messages";

        baseUrl = baseUrl + "?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String";
        
        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("GET");

        connection.setRequestProperty("AccountReference", "your-AccountReference-header-here");
        
        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/requests/%7BrequestId%7D/messages";

        url += "?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String";

        var request = new HttpRequestMessage(HttpMethod.Parse("GET"), url);

        request.Headers.Add("AccountReference", "your-AccountReference-header-here");
        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/requests/%7BrequestId%7D/messages';
$ch = curl_init();

$baseUrl =  $baseUrl . '?AccountReference=String&MessageId=String&ConversationId=String&Status=String&Reason=String&Msisdn=String&DestinationCode=String&Originator=String&RequestName=String&Channel=String&MessageType=String&Source=String&Route=String&StartTime=String&EndTime=String&PageSize=Integer&PageNo=Integer&SortOrder=String&SortBy=String&RetrieveMessageBody=Boolean&Direction=String&SubmittedBy=String&RcsType=String';

curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'AccountReference' => 'your-AccountReference-header-here',
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```

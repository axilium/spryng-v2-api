# Contacts / Get all

Source: <https://developer.spryng.nl/contacts/v2-contacts-get-all>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint allows you to retrieve a list of all the data for your contacts associated with your account in a single API call. The request can be filtered by optOutStatus, sortBy, sortOrder, search, destination and groupId as well as other properties listed below. This is the ideal tool for synchronizing your contact list from another system, such as a CRM, an e-commerce platform, or a customer database, ensuring your contacts are always up-to-date.

The documentation in this section contains the core reference information you will need to retrieve contacts via our API, including required headers, cURL sample & error codes.

Before you start using this endpoint, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

```
https://api.spryng.nl/v2/contacts/filter
```

**Method: GET**

## Headers

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| AccountReference | String | Your Spryng account reference - e.g. SPNL0000000 |
| X-Api-Key | String | API Key value for authorization |

**Example request header:**

```
GET https://api.spryng.nl/v2/contacts/filter
-H 'Api-Key: YOUR_API_KEY'
-H 'AccountReference: SPNL0000000'
```

**Optional input path parameters**

The below parameters are optional within the path, they are designed to provide you further capability to filter your requests.

| Parameter | Type | Description |
| --- | --- | --- |
| optOutStatus | Query | Filter contacts by the optOutStatus. Allowed values are: subscribed or unsubscribed |
| sortBy | Query | Sort contacts into grouped data. Available values: Name \| createdAt |
| sortOrder | Query | Sets the sorting direction. Default= descending. Available values: Asc & Desc |
| search | Query | Ability to search using the following: address \| first name \| last name \| quick name. Searching will retrieve information from all parameters |
| destination | Query | Filter contacts by the destination. Value should be a valid ISO3166 country code. For example: GB \| DE \| IT \| US |
| groupId | Query | Filter contacts by a certain groupId only |
| contactOwnership | Query | Specifies the ownership type of a contact to filter by. Possible values: Shared \| Owned \| All. Default value = All |

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide.](https://restfulapi.net/http-status-codes/)

**Success**

```
200 OK
```

Success response with the following example response.

```
{
    "data": {
    "contacts": [
      {
        "contactId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
        "accountReference": "SPNL0000000",
        "firstName": "string",
        "lastName": "string",
        "quickName": "string",
        "lastUpdatedBy": "string",
        "smsoptOutStatus": "Subscribed",
        "createTimestamp": "2025-05-26T12:37:30.770Z",
        "lastUpdateTimestamp": "2025-05-26T12:37:30.770Z",
        "addresses": [
          {
            "addressId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
            "addressType": "string",
            "addressValue": "string",
            "destinationCode": "string",
            "optOutStatus": "Subscribed"
          }
        ],
        "contactMetadata": {
          "additionalProp1": "string",
          "additionalProp2": "string",
          "additionalProp3": "string"
        }
      }
    ],
    "totalCount": 10,
    "totalPages": 2,
    "currentPage": 1
  }
}
```

**Error codes**

```
400 Bad Request
```

The server couldn't process the request due to the accountReference missing from the header or is invalid.

Response example

```
{
    "errors": [
        {
            "errorCode": "error code",
            "errorMessage": "error message"
        }
    ]
}
```

```
401 Unauthorized
```

The server couldn't process the request as the authorization header is missin or invalid.

Response example

```
{
    "errors": [
        {
            "errorCode": "unauthenticatedError",
            "errorMessage": "Request for authenticated route '/v2/contacts/filter' was unauthenticated"
        }
    ]
}
```

## Code sample: Curl

```bash
curl 'https://api.spryng.nl/v2/contacts/filter?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer' -H 'X-Api-Key: {{your-key-here}}' -H 'AccountReference: {{your-AccountReference-header-here}}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/contacts/filter";

  url+= "?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer";

  const headers = {
    "AccountReference": "your-AccountReference-header-here",
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  try {
    const response = await axios.get(
        url,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'AccountReference': 'your-AccountReference-header-here',
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/contacts/filter"

baseUrl = f"{baseUrl}?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer"


try:
    response = requests.get( baseUrl,  headers = headers )
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/contacts/filter";

        baseUrl = baseUrl + "?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer";
        
        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("GET");

        connection.setRequestProperty("AccountReference", "your-AccountReference-header-here");
        
        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/contacts/filter";

        url += "?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer";

        var request = new HttpRequestMessage(HttpMethod.Parse("GET"), url);

        request.Headers.Add("AccountReference", "your-AccountReference-header-here");
        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/contacts/filter';
$ch = curl_init();

$baseUrl =  $baseUrl . '?AccountReference=String&GroupId=Array&Destination=String&OptOutStatus=String&AddressType=String&MetadataKey=String&MetadataValue=String&Search=String&ContactOwnership=String&SortBy=String&SortOrder=String&PageNo=Integer&PageSize=Integer';

curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'AccountReference' => 'your-AccountReference-header-here',
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```

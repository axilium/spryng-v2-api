# Contacts / Bulk update/create

Source: <https://developer.spryng.nl/contacts/v2-contacts-bulk-create-or-update>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint allows you to add or edit a list of contacts in bulk with a single API call. This is the ideal tool for synchronizing your contact list from another system, such as a CRM, an e-commerce platform, or a customer database, ensuring your contacts are always up-to-date.

The documentation in this section contains the core reference information you will need to create contacts via our API, including required headers, cURL sample & error codes.

Before you start using this endpoint, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

```
https://api.spryng.nl/v2/contacts/bulk
```

**Method: PUT**

## Headers

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| Content-Type | String | application/json |
| X-Api-Key | String | API Key value for authorization |

**Example request header:**

```
PUT https://api.spryng.nl/v2/contacts/bulk
-H 'Api-Key: YOUR_API_KEY'
-H 'Content-Type: application/json'
```

## Request body

You can use the cURL sample provided on the right hand side of this page to directly copy/paste into your chosen integration tool, using the below properties to modify your request as required for your business needs.

**Required, or conditional, request body parameters**

If a required parameter is not provided, or incorrect, the request will fail. If a parameter is conditional it is only required for certain channels or is dependent on the desired format of the message.

| Parameter | Type | Required or Conditional | Description |
| --- | --- | --- | --- |
| accountReference | String | Required | Your Spryng account reference - e.g. SPNL0000000 |
| contactsId | GUID | Conditional | ContactId if updating a user e.g. "68890cd2-7d9d-43c4-8fc9-8de20160e76f" |
| contacts.firstName | String | Required | The first name of the contact. Max length 500 characters |
| contacts.lastName | String | Required | The last name of the contact. Max length 500 characters |
| contacts.addressType | JSON object | Conditional | The type of address to be stored e.g. MSISDN \| Email |
| contacts.addressValue | JSON value | Conditional | The email address or mobile number to be stored |

**Optional request body parameters**

These parameters are not mandatory within the request body, they are designed to provide you further capability to customise or filter your requests.

| Parameter | Type | Description |
| --- | --- | --- |
| contacts.quickName | String | The short name of the contact.Max 500 characters |
| contacts.optOutStatus | JSON Value | A binary value to determine if the user is opted out of marketing communications. Accepted values are subscribed or unsubscribed |
| contacts.contactMetadata | Object | Provided as an object e.g. “team” : “your data added here”. These are saved with the message and will be returned when exporting the message history. This can make some data analysis easier |
| contacts.groups | JSON array | Can be used to add the contact to group(s). If using this parameter a valid groupId must be provided |

**Request body example**

```
{
    "accountReference": "SPNL0000000",
    "contacts": [
        {
            "firstName": "John",
            "lastName": "Mclane",
            "quickName": "Roy",
            "addresses": [
                {
                    "addressType": "Msisdn",
                    "addressValue": "+447711000039"
                }
            ],
            "contactMetadata": {
                "team": "octopus"
            },
            "groups": [
                "a56cd83a-f58f-4f07-be76-164c2d734787"
            ]
        },
        {
            "firstName": "Lisa",
            "lastName": "Mclane",
            "quickName": "Mclisa",
            "addresses": [
                {
                    "addressType": "Msisdn",
                    "addressValue": "+447711000031",
                    "optOutStatus": "Subscribed"
                }
            ],
            "contactMetadata": {
                "team": "octopus"
            },
            "groups": [
                "a56cd83a-f58f-4f07-be76-164c2d734787"
            ]
        }
    ]
}
```

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide.](https://restfulapi.net/http-status-codes/)

**Success**

```
200 OK
```

Success response with the following example response.

```
{
    "data": {
        "accountReference": "SPNL0000000",
        "contacts": [
            {
                "id": "5e89f37d-a101-4ac1-ba8e-736f18641432",
                "firstName": "John",
                "lastName": "Mclane",
                "quickName": "Roy",
                "addresses": [
                    {
                        "addressType": "MSISDN",
                        "addressValue": "+447711000039"
                    }
                ],
                "contactMetadata": {
                    "team": "octopus"
                },
                "result": "success",
                "reasons": []
            },
            {
                "id": "6243c3b6-23b0-48a1-baf8-3bea83a473d7",
                "firstName": "Lisa",
                "lastName": "Mclane",
                "quickName": "Mclisa",
                "addresses": [
                    {
                        "addressType": "MSISDN",
                        "addressValue": "+447711000031"
                    }
                ],
                "contactMetadata": {
                    "team": "octopus"
                },
                "result": "success",
                "reasons": []
            }
        ]
    }
}
```

**Error codes**

```
400 Bad Request
```

The server couldn't process your request as one or more of the required fields are missing or have an invalid value.

Response example

```
{ 
    "errors": [
        {
            "errorCode": "error code",
            "errorMessage": "error message"
        }
    ]
}
```

```
401 Unauthorized
```

The server couldn't process the request as the authenication header is missing or invalid.

Response example

```
{
    "title": "An error occurred while processing the request",
    "status": 401,
    "message": "Request for authenticated route '/v2/contacts/bulk' was unauthenticated",
    "errors": {},
    "internalErrors": [
        {
            "message": "Pipeline",
            "metadata": {
                "unauthenticatedError": "Request for authenticated route '/v2/contacts/bulk' was unauthenticated"
            }
        }
    ]
}
```

## Code sample: Curl

```bash
curl -X PATCH 'https://api.spryng.nl/v2/contacts/bulk' -H 'X-Api-Key: {{your-key-here}}' -H 'Content-Type: application/json' -d '{
  "accountReference": "SPNLxxxxxx",
  "contacts": [
    {
      "firstName": "firstName",
      "lastName": "lastName",
      "quickName": "quickName",
      "addresses": [
        {
          "addressType": "addressType",
          "addressValue": "addressValue",
          "optOutStatus": "optOutStatus"
        }
      ],
      "contactMetadata": {},
      "groups": [
        ""
      ]
    }
  ]
}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/contacts/bulk";

  const headers = {
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  const body = `{
  "accountReference": "SPNLxxxxxx",
  "contacts": [
    {
      "firstName": "firstName",
      "lastName": "lastName",
      "quickName": "quickName",
      "addresses": [
        {
          "addressType": "addressType",
          "addressValue": "addressValue",
          "optOutStatus": "optOutStatus"
        }
      ],
      "contactMetadata": {},
      "groups": [
        ""
      ]
    }
  ]
}`;
    
  try {
    const response = await axios.patch(
        url,
        body,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/contacts/bulk"



body = """{
  "accountReference": "SPNLxxxxxx",
  "contacts": [
    {
      "firstName": "firstName",
      "lastName": "lastName",
      "quickName": "quickName",
      "addresses": [
        {
          "addressType": "addressType",
          "addressValue": "addressValue",
          "optOutStatus": "optOutStatus"
        }
      ],
      "contactMetadata": {},
      "groups": [
        ""
      ]
    }
  ]
}"""
try:
    response = requests.patch( baseUrl,  data = body)
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/contacts/bulk";

        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("PATCH");

        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        String body = """
        {
  "accountReference": "SPNLxxxxxx",
  "contacts": [
    {
      "firstName": "firstName",
      "lastName": "lastName",
      "quickName": "quickName",
      "addresses": [
        {
          "addressType": "addressType",
          "addressValue": "addressValue",
          "optOutStatus": "optOutStatus"
        }
      ],
      "contactMetadata": {},
      "groups": [
        ""
      ]
    }
  ]
}""";

        connection.setDoOutput(true);

        try (OutputStream outputStream = connection.getOutputStream()) {
            byte[] input = body.getBytes(StandardCharsets.UTF_8);
            outputStream.write(input, 0, input.length);
        }
        catch (Exception e) {
            System.err.println("Failed due to error: " + e.getMessage());
        }
        
        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/contacts/bulk";


        var request = new HttpRequestMessage(HttpMethod.Parse("PATCH"), url);

        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var body = @"{
  ""accountReference"": ""SPNLxxxxxx"",
  ""contacts"": [
    {
      ""firstName"": ""firstName"",
      ""lastName"": ""lastName"",
      ""quickName"": ""quickName"",
      ""addresses"": [
        {
          ""addressType"": ""addressType"",
          ""addressValue"": ""addressValue"",
          ""optOutStatus"": ""optOutStatus""
        }
      ],
      ""contactMetadata"": {},
      ""groups"": [
        """"
      ]
    }
  ]
}"; 
        
        request.Content = new StringContent(body, Encoding.UTF8, "application/json");
                
        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/contacts/bulk';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


    $body = json_encode([
  "accountReference" => "SPNLxxxxxx",
  "contacts" => [
    [
      "firstName" => "firstName",
      "lastName" => "lastName",
      "quickName" => "quickName",
      "addresses" => [
        [
          "addressType" => "addressType",
          "addressValue" => "addressValue",
          "optOutStatus" => "optOutStatus"
        ]
      ],
      "contactMetadata" => new stdClass(),
      "groups" => [
        ""
      ]
    ]
  ]
]);
        
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```

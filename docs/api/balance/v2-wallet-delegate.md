# Balance / Share

Source: <https://developer.spryng.nl/balance/v2-wallet-delegate>

Generated by `tools/fetch-docs.php`. Do not edit by hand.

## API overview

This endpoint allows you to delegate a balance to a sub-account specified in the header of the request.

Before you start your implementation, please check you have your Account, API Key and Base URL set up and have familiarised yourself with our implementation prerequisites.

## URL & method

**Endpoint URL**

```
https://api.spryng.nl/v2/wallet/delegate
```

**Method: POST**

## Headers

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| Content-Type | String | application/json |
| X-Api-Key | String | API Key value for authorization |
| AccountReference | String | Your Esendex account reference - e.g. SPNL0000000 |

**Example request header**

```
POST https://api.spryng.nl/v2/wallet/delegate
-H 'Api-Key: YOUR_API_KEY'
-H 'Content-Type: application/json'
-H 'AccountReference: SPNL0000000'
```

## Headers & parameters

These parameters are mandatory to be provided in the header of your request. If not provided, or incorrect, your request will fail.

| Header | Type | Description |
| --- | --- | --- |
| Content-Type | String | application/json |
| X-Api-Key | String | API Key value for authorization |
| AccountReference | String | Your Spryng account reference - e.g. SPNL0000000 |

**Example request header**

```
POST https://api.spryng.nl/v2/wallet/delegate
-H 'Api-Key: YOUR_API_KEY'
-H 'Content-Type: application/json'
-H 'AccountReference: SPNL0000000'
```

## Request body

You can use the cURL sample provided on the right hand side of this page to directly copy/paste into your chosen integration tool, using the below properties to modify your request as required for your business needs.

**Required, or conditional, request body parameters**

If a required parameter is not provided, or incorrect, the request will fail. If a parameter is conditional, it is only required for certain channels or is dependent on the desired format of the message.

| Parameter | Type | Required or conditional | Description |
| --- | --- | --- | --- |
| delegations.subaccountReference | Required | String | The desired Spryng subaccount reference to send the balance to - e.g. SPNL0000000 |
| delegations.amount | Required | Integer | Amount of funds to delegate |

**Request body example**

```
{
  "delegations": [
    {
      "subaccountReference": "subaccountReference",
      "amount": 0.1
    }
  ]
}
```

## Responses

The standard responses for this API are listed below. For a full list of possible REST API status codes, please refer to [this guide](https://restfulapi.net/http-status-codes/).

**Success**

```
200 OK
```

The request succeeded, and a new resource was created as a result.

**Error codes**

```
400 Bad Request
```

The server could not understand the request due to incorrect syntax or an incorrect/missing required property. You should not repeat the request without modifications.

```
{
  "errors": [
    {
      "errorCode": "string",
      "errorMessage": "string"
    }
  ]
}
```

```
401 Unauthorized
```

Indicates that the request has failed due to missing or incorrect authentication information. You can repeat the request with a suitable authorization header

```
{
  "errors": [
    {
      "errorCode": "string",
      "errorMessage": "string"
    }
  ]
}
```

```
403 Forbidden
```

Indicates that the request has been understood but the user is not an admin or has an invalid account relationship.

```
{
  "errors": [
    {
      "errorCode": "string",
      "errorMessage": "string"
    }
  ]
}
```

## Code sample: Curl

```bash
curl -X POST 'https://api.spryng.nl/v2/wallet/delegate' -H 'X-Api-Key: {{your-key-here}}' -H 'AccountReference: {{your-AccountReference-header-here}}' -H 'Content-Type: application/json' -d '{
  "delegations": [
    {
      "subAccountReference": "subAccountReference",
      "amount": 0.0
    }
  ]
}'
```

## Code sample: NodeJS

```javascript
async function main() {
  const axios = require('axios');

  let url = "https://api.spryng.nl/v2/wallet/delegate";

  const headers = {
    "AccountReference": "your-AccountReference-header-here",
    "X-Api-Key": "YOUR_KEY_HERE"
    };


  const body = `{
  "delegations": [
    {
      "subAccountReference": "subAccountReference",
      "amount": 0.0
    }
  ]
}`;
    
  try {
    const response = await axios.post(
        url,
        body,
        { headers }
    );

    console.log("Response:", response.data);
  } catch (error) {
    if (error.response) {
      console.error(`Error: ${error.response.status}`);
      console.error("Error details:", error.response.data);
    } else {
      console.error("Error:", error.message);
    }
  }
}
```

## Code sample: Python

```python
import requests
from urllib.parse import urlencode

headers = {
      'AccountReference': 'your-AccountReference-header-here',
      'X-Api-Key': 'YOUR_KEY_HERE'
    }

baseUrl = "https://api.spryng.nl/v2/wallet/delegate"



body = """{
  "delegations": [
    {
      "subAccountReference": "subAccountReference",
      "amount": 0.0
    }
  ]
}"""
try:
    response = requests.post( baseUrl,  headers = headers,  data = body)
        
    if response.ok:
            print("Success!")
            print(response.json())
    else:
        print(f"Error! HTTP {response.status_code}")
        print(response.text)

except requests.RequestException as e:
    print(f"Request failed: {e}")
```

## Code sample: Java

```java
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URI;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws Exception {
        String baseUrl = "https://api.spryng.nl/v2/wallet/delegate";

        URL url = URI.create(baseUrl).toURL();
        HttpURLConnection connection = (HttpURLConnection) url.openConnection();
        connection.setRequestMethod("POST");

        connection.setRequestProperty("AccountReference", "your-AccountReference-header-here");
        
        connection.setRequestProperty("X-Api-Key", "YOUR_KEY_HERE");

        String body = """
        {
  "delegations": [
    {
      "subAccountReference": "subAccountReference",
      "amount": 0.0
    }
  ]
}""";

        connection.setDoOutput(true);

        try (OutputStream outputStream = connection.getOutputStream()) {
            byte[] input = body.getBytes(StandardCharsets.UTF_8);
            outputStream.write(input, 0, input.length);
        }
        catch (Exception e) {
            System.err.println("Failed due to error: " + e.getMessage());
        }
        
        int responseCode = connection.getResponseCode();

        if (responseCode >= 200 && responseCode < 300) {
          try (var reader = new java.io.BufferedReader(
            new java.io.InputStreamReader(connection.getInputStream(), StandardCharsets.UTF_8))) {
              StringBuilder response = new StringBuilder();
              String line;

              while ((line = reader.readLine()) != null) {
                  response.append(line.trim());
              }
            }
        }
        else {
          try (var errorReader = new java.io.BufferedReader(new java.io.InputStreamReader(connection.getErrorStream(), StandardCharsets.UTF_8))) {
            StringBuilder errorResponse = new StringBuilder();
            String line;

            while ((line = errorReader.readLine()) != null) {
                errorResponse.append(line.trim());
            }

            System.err.println("Error Response: " + errorResponse.toString());
          } catch (Exception e) {
            System.err.println("Failed to read error response: " + e.getMessage());
          }
        }

        connection.disconnect();
    }
}
```

## Code sample: C#

```csharp
using System.Text;

namespace ApiRequest;
class Program
{
    public async Task Main()
    {
        var httpClient = new HttpClient();
        var url = "https://api.spryng.nl/v2/wallet/delegate";


        var request = new HttpRequestMessage(HttpMethod.Parse("POST"), url);

        request.Headers.Add("AccountReference", "your-AccountReference-header-here");
        request.Headers.Add("X-Api-Key", "YOUR_KEY_HERE");


        var body = @"{
  ""delegations"": [
    {
      ""subAccountReference"": ""subAccountReference"",
      ""amount"": 0.0
    }
  ]
}"; 
        
        request.Content = new StringContent(body, Encoding.UTF8, "application/json");
                
        var response = await httpClient.SendAsync(request);

        if(response.IsSuccessStatusCode)
        {
            var responseBody = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Response: " + responseBody);
        }
        else
        {
            Console.WriteLine($"Error: {response.StatusCode}");
            var errorContent = await response.Content.ReadAsStringAsync();
            Console.WriteLine("Error details: " + errorContent);
        }
    }
}
```

## Code sample: PHP

```php
<?php

$baseUrl = 'https://api.spryng.nl/v2/wallet/delegate';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $baseUrl);

$headers = [
     'AccountReference' => 'your-AccountReference-header-here',
     'X-Api-Key' => 'YOUR_KEY_HERE'
    ];

curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);


    $body = json_encode([
  "delegations" => [
    [
      "subAccountReference" => "subAccountReference",
      "amount" => 0.0
    ]
  ]
]);
        
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$info = curl_getinfo($ch);

curl_close($ch);

if ($info['http_code'] >= 400) {
    echo 'Error! HTTP code: ' . $info['http_code'] . ', Response: ' . $response;
} else {
    echo 'Success! Response: ' . $response;
}
?>
```
